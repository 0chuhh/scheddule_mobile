import 'package:flutter/material.dart';

class BellScheduleScreen extends StatelessWidget {
  const BellScheduleScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Bell schedule');
  }
}
